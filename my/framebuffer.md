В Vulkan **фреймбуфер (Framebuffer)** — это объект (`VkFramebuffer`), который выступает в роли связующего звена между **Render Pass** (проходом рендеринга) и реальными изображениями в памяти (**Image Views**), в которые будет производиться отрисовка.
